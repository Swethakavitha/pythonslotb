def print_pattern(rows):
    for i in range(1, rows + 1):
        # Print numbers from 2 up to i * 2 (inclusive)
        for j in range(1, i + 1):
            print(2 * j, end=" ")
        print()  # Move to the next line after each row

# Test the function with the given pattern
print_pattern(5)
